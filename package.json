{
  "name": "weaver",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm --filter frontend install && pnpm --filter frontend run build",
    "start": "node backend/server.js",
    "dev": "concurrently \"pnpm --filter frontend dev\" \"nodemon backend/server.js\""
  },
  "devDependencies": {
    "concurrently": "^8.2.1",
    "nodemon": "^3.1.10"
  }
}
